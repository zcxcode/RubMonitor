export const addCurrencies = (curr) => ({ type: "ADD_CURRENCIES", content: curr });

export const addHistory = (curr) => ({ type: "ADD_HISTORY", content: curr });

export const filterCurrency = (curr) => ({ type: "FILTER_CURRENCIES", content: curr });

export const filterInput = (curr) => ({ type: "FILTER_INPUT", content: curr });
